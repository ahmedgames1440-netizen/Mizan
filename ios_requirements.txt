# متطلبات نسخة iOS — تطبيق ميزان
#
# هذا الملف توثيقي بحت (يُستخدم كمفتاح كاش بـ GitHub Actions)، وليس ملف
# pip requirements فعلي يُثبَّت مباشرة — التثبيت الفعلي يتم عبر
# `toolchain pip install` بداخل kivy-ios toolchain.

# ===== المرحلة 1 (الحالية): النسخة الأساسية =====
# مكتبات بايثون خالصة، تُثبّت بأمان عبر `toolchain pip install` بدون
# الحاجة لـ recipe مخصص:
openpyxl
arabic-reshaper
python-bidi
plyer

# ===== المرحلة 2 (مستقبلية): الرسوم البيانية وتصدير PDF =====
# هذي تحتاج recipes مخصصة في kivy-ios قبل إضافتها هنا، لأنها تحتوي كود C/
# Cython ولا تُثبّت بـ pip العادي على iOS:
#   - numpy      → recipe موجود لكنه معروف بمشاكل بناء متكررة (cython errors)
#   - matplotlib → لا يوجد recipe رسمي مستقر لـ kivy-ios حتى الآن
#   - reportlab  → يحتاج تصحيح مشابه لما طبّقناه بنسخة أندرويد
#                  (إزالة استخدام base64.encodestring المهجور)
#
# عند العمل على هذي المرحلة: أضف الأسماء هنا بعد التحقق من وجود recipe
# يعمل فعليًا، لتحديث مفتاح كاش الـ workflow تلقائيًا.
